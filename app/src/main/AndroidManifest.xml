<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lusr.pig">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 联网 -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/apps"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Main">
        <activity
            android:name=".mine.FeedBack"
            android:theme="@style/CustomActionBarTheme"></activity>
        <activity
            android:name=".mine.ChangePassWord"
            android:theme="@style/CustomActionBarTheme" />
        <activity
            android:name=".mine.Login"
            android:theme="@style/CustomActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mine.Warn"
            android:theme="@style/CustomActionBarTheme">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN" />-->


            <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".firstpage.AddParam"
            android:theme="@style/CustomActionBarTheme"/>
        <activity
            android:name=".anno.Page"
            android:theme="@style/CustomActionBarTheme" />
        <activity
            android:name=".firstpage.RoomBeanScan"
            android:theme="@style/CustomActionBarTheme" />

        <activity
            android:name=".firstpage.AddHome"
            android:theme="@style/CustomActionBarTheme" />
        <activity android:name=".MainActivity">

            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN" />-->


            <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>

    </application>

</manifest>